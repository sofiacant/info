Leo la información toda junta pero cambia para almacenar, los mensajes van a la lsita de mensajes y los datos del cliente al nodo del árbol.
cada nodo del árbol va a tener el código de cliente, email y lista de mensajes de ese cliente
construyo un árbol que tiene como dato un registro cliente que tiene

1ro tengp que buscar en el árbol el cliente al que le corresponde el msj, si lo encuentro, le agrego la inforamcion de los mensajes a su lista de mensajes

si el árbol esta vacio quiere decir que todavía no había leído un mensaje de ese cliente, entonces creo un nodo del árbol, cargar los datos del clientes, inicializar la lista y agrego el primer mensaje

caso: encontre un cliente que tiene el mismo código, tonces le agrego el mensaje

todos los correos sin leer de la lista de cliente actual, llamo a ot5ra función que dada la lista de mensajes deun cliente cuenta cuantos son desde una direccion

a eso tengo que sumarle todos los mensajes sin leer de todos los clientes dei izquierdo, y a eso le sumo todos del árbol derecho